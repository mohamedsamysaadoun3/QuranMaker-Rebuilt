package hazem.nurmontage.videoquran

import androidx.activity.enableEdgeToEdge

/**
 * Edge-to-edge display helper
 */
object EdgeToEdge {
    fun enable(activity: androidx.appcompat.app.AppCompatActivity) {
        activity.enableEdgeToEdge()
    }
}
